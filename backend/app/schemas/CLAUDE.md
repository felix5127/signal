# backend/app/schemas/
> L2 | 父级: backend/app/CLAUDE.md

## 职责
Pydantic 数据传输对象，用于 API 请求/响应的数据验证和序列化。

## 成员清单
signal.py: Signal 相关 schema (SignalResponse, SignalList, SignalCreate 等)
filter.py: FilterResult schema，LLM 过滤结果模型

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
