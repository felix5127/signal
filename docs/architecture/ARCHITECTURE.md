# Signal Hunter 系统架构文档

> 版本: v3.0 | 更新时间: 2026-01-29

## 一、系统概览

### 1.1 架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户层                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │
│  │   Web 前端    │  │   RSS 订阅   │  │   API 调用   │                  │
│  │  (Next.js)   │  │   (/feeds)   │  │  (REST API)  │                  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘                  │
└─────────┼─────────────────┼─────────────────┼───────────────────────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            API 网关层                                    │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    FastAPI Application                           │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │   │
│  │  │Resources│ │Research │ │ Podcast │ │  Admin  │ │  Auth   │   │   │
│  │  │   API   │ │   API   │ │   API   │ │   API   │ │   API   │   │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           业务逻辑层                                     │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐               │
│  │   Services    │  │  Processors   │  │   Scrapers    │               │
│  │  ┌─────────┐  │  │  ┌─────────┐  │  │  ┌─────────┐  │               │
│  │  │Resource │  │  │  │Analyzer │  │  │  │   RSS   │  │               │
│  │  │ Service │  │  │  │(3-step) │  │  │  │ Scraper │  │               │
│  │  ├─────────┤  │  │  ├─────────┤  │  │  ├─────────┤  │               │
│  │  │Research │  │  │  │Translator│ │  │  │ Podcast │  │               │
│  │  │ Service │  │  │  │(3-step) │  │  │  │ Scraper │  │               │
│  │  ├─────────┤  │  │  ├─────────┤  │  │  ├─────────┤  │               │
│  │  │  Deep   │  │  │  │Transcriber│ │  │  │  Video  │  │               │
│  │  │Research │  │  │  │(Tingwu) │  │  │  │ Scraper │  │               │
│  │  ├─────────┤  │  │  ├─────────┤  │  │  ├─────────┤  │               │
│  │  │ Export  │  │  │  │ Filter  │  │  │  │ Twitter │  │               │
│  │  │ Service │  │  │  │(Rule+LLM)│ │  │  │ Scraper │  │               │
│  │  └─────────┘  │  │  └─────────┘  │  │  └─────────┘  │               │
│  └───────────────┘  └───────────────┘  └───────────────┘               │
└─────────────────────────────────────────────────────────────────────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           数据存储层                                     │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐               │
│  │  PostgreSQL   │  │    Redis      │  │ Cloudflare R2 │               │
│  │  + pgvector   │  │    Cache      │  │    Storage    │               │
│  │  ┌─────────┐  │  │  ┌─────────┐  │  │  ┌─────────┐  │               │
│  │  │Resources│  │  │  │API Cache│  │  │  │  Audio  │  │               │
│  │  │Research │  │  │  │Sessions │  │  │  │ Reports │  │               │
│  │  │ Tasks   │  │  │  │Rate Limit│ │  │  │ Exports │  │               │
│  │  └─────────┘  │  │  └─────────┘  │  │  └─────────┘  │               │
│  └───────────────┘  └───────────────┘  └───────────────┘               │
└─────────────────────────────────────────────────────────────────────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          外部服务层                                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐      │
│  │OpenRouter│  │ Tavily  │  │ Tingwu  │  │DashScope│  │ XGo.ing │      │
│  │  (LLM)   │  │ (Search)│  │(Transcr)│  │  (TTS)  │  │(Twitter)│      │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘  └─────────┘      │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈总览

| 层级 | 技术选型 | 版本 |
|------|---------|------|
| 前端框架 | Next.js (App Router) | 14.2.0 |
| UI 组件库 | shadcn/ui + TailwindCSS | 3.4.0 |
| 动画库 | Framer Motion | 11.x |
| 后端框架 | FastAPI | 0.115+ |
| ORM | SQLAlchemy | 2.0+ |
| 数据库 | PostgreSQL + pgvector | 15 |
| 缓存 | Redis | 7 |
| 任务调度 | APScheduler | 3.x |
| 容器化 | Docker + Docker Compose | - |

---

## 二、后端架构

### 2.1 目录结构

```
backend/
├── app/
│   ├── main.py              # FastAPI 应用入口
│   ├── config.py            # 配置加载器 (YAML + ENV)
│   ├── database.py          # 数据库连接
│   ├── scheduler_jobs.py    # 定时任务定义
│   │
│   ├── api/                 # REST API 路由 (16 模块)
│   │   ├── resources.py     # 资源 CRUD + 搜索
│   │   ├── research.py      # 研究助手 API
│   │   ├── podcast.py       # 播客生成 API
│   │   ├── auth.py          # 用户认证
│   │   ├── admin/           # 管理后台 API
│   │   └── ...
│   │
│   ├── models/              # SQLAlchemy ORM 模型 (14 模型)
│   │   ├── resource.py      # 资源模型
│   │   ├── research.py      # 研究助手模型
│   │   └── ...
│   │
│   ├── services/            # 业务服务层 (18 服务)
│   │   ├── resource_service.py
│   │   ├── deep_research_service.py
│   │   ├── cache_service.py
│   │   └── ...
│   │
│   ├── processors/          # 内容处理引擎 (11 处理器)
│   │   ├── analyzer.py      # LLM 三步分析
│   │   ├── translator.py    # 三步翻译
│   │   ├── transcriber.py   # 音频转写
│   │   ├── filter.py        # 规则过滤
│   │   └── deep_research/   # 深度研究子模块
│   │
│   ├── scrapers/            # 数据采集器 (15 爬虫)
│   │   ├── rss.py           # RSS 通用爬虫
│   │   ├── podcast.py       # 播客爬虫
│   │   ├── video.py         # 视频爬虫
│   │   └── ...
│   │
│   └── tasks/               # 异步任务 (5 模块)
│       ├── pipeline.py      # 主采集流水线
│       ├── digest.py        # 汇总生成
│       └── newsletter.py    # 周刊生成
│
├── config.yaml              # 配置文件
├── requirements.txt         # Python 依赖
└── Dockerfile
```

### 2.2 分层架构

```
┌────────────────────────────────────────────────────────────┐
│                      API Layer (api/)                       │
│  - HTTP 请求处理                                            │
│  - 参数验证 (Pydantic)                                      │
│  - 响应序列化                                               │
└────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────┐
│                   Service Layer (services/)                 │
│  - 业务逻辑编排                                             │
│  - 事务管理                                                 │
│  - 跨模块协调                                               │
└────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────┐
│                 Processor Layer (processors/)               │
│  - 单一职责处理                                             │
│  - LLM 调用封装                                             │
│  - 内容转换                                                 │
└────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────┐
│                  Scraper Layer (scrapers/)                  │
│  - 数据源适配                                               │
│  - HTTP 请求                                                │
│  - 数据解析                                                 │
└────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────┐
│                   Model Layer (models/)                     │
│  - ORM 定义                                                 │
│  - 数据验证                                                 │
│  - 关系映射                                                 │
└────────────────────────────────────────────────────────────┘
```

### 2.3 数据采集流水线

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         数据采集流水线                                   │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  OPML    │───▶│   RSS    │───▶│  Initial │───▶│   Rule   │───▶│   LLM    │
│  解析    │    │  采集    │    │  Filter  │    │  Filter  │    │  分析    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
                                     │               │               │
                                     ▼               ▼               ▼
                              ┌──────────┐    ┌──────────┐    ┌──────────┐
                              │   去重   │    │ 语言识别 │    │  三步    │
                              │ (URL Hash)│    │ 关键词匹配│    │ 深度分析 │
                              └──────────┘    └──────────┘    └──────────┘
                                                                    │
                                                                    ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│   API    │◀───│  数据库  │◀───│   翻译   │◀───│   评分   │◀───│ 摘要生成 │
│   暴露   │    │   存储   │    │  (可选)  │    │  (0-100) │    │ 观点提取 │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
```

### 2.4 LLM 处理流程

#### 三步深度分析

```
Step 1: 全文分析
┌─────────────────────────────────────────┐
│ Input: 原始内容 (HTML/Markdown)         │
│ Output: 初步分析结果                    │
│ - 一句话总结                            │
│ - 详细摘要                              │
│ - 主要观点                              │
│ - 金句提取                              │
│ - 领域分类                              │
│ - 初步评分                              │
└─────────────────────────────────────────┘
                    │
                    ▼
Step 2: 检查反思
┌─────────────────────────────────────────┐
│ Input: Step 1 输出 + 原始内容           │
│ Output: 反思与改进建议                  │
│ - 遗漏要点                              │
│ - 评分合理性                            │
│ - 分类准确性                            │
│ - 改进方向                              │
└─────────────────────────────────────────┘
                    │
                    ▼
Step 3: 优化改进
┌─────────────────────────────────────────┐
│ Input: Step 1 输出 + Step 2 反思        │
│ Output: 最终优化结果                    │
│ - 优化后摘要                            │
│ - 修正后评分                            │
│ - 补充观点                              │
│ - 最终分类                              │
└─────────────────────────────────────────┘
```

#### 三步翻译

```
Step 1: 直译 → Step 2: 反思 → Step 3: 意译优化
```

### 2.5 缓存策略

| 数据类型 | TTL | 说明 |
|---------|-----|------|
| 资源列表 | 5 分钟 | 高频访问，短缓存 |
| 资源详情 | 10 分钟 | 中频访问 |
| 统计数据 | 1 分钟 | 实时性要求高 |
| 标签列表 | 30 分钟 | 变化少 |
| 深度研究报告 | 24 小时 | 生成成本高 |

---

## 三、前端架构

### 3.1 目录结构

```
frontend/
├── app/                     # Next.js App Router
│   ├── page.tsx             # 首页
│   ├── articles/            # 文章列表
│   ├── podcasts/            # 播客列表
│   ├── videos/              # 视频列表
│   ├── tweets/              # 推文列表
│   ├── featured/            # 精选内容
│   ├── resources/[id]/      # 详情页
│   ├── research/            # 研究助手
│   ├── admin/               # 管理后台
│   ├── layout.tsx           # 根布局
│   └── globals.css          # 全局样式
│
├── components/
│   ├── ui/                  # shadcn/ui 组件 (30+)
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── input.tsx
│   │   └── ...
│   │
│   ├── detail/              # 详情页组件
│   │   ├── ai-summary-card.tsx
│   │   ├── ai-assistant-card.tsx
│   │   └── related-content-card.tsx
│   │
│   ├── research/            # 研究助手组件
│   │   ├── workspace.tsx
│   │   ├── project-list.tsx
│   │   └── panels/
│   │
│   ├── podcast/             # 播客组件
│   │   ├── audio-player.tsx
│   │   └── podcast-detail.tsx
│   │
│   ├── landing/             # Landing 组件
│   │   └── Hero.tsx
│   │
│   ├── navbar.tsx           # 导航栏
│   ├── Footer.tsx           # 页脚
│   └── ...                  # 其他组件
│
└── lib/
    ├── api.ts               # API 客户端
    ├── utils.ts             # 工具函数
    ├── motion.ts            # 动画预设
    └── design-system/       # 设计令牌
        └── tokens/
            ├── colors.ts
            ├── spacing.ts
            └── effects.ts
```

### 3.2 设计系统架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           Design System                                  │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│    Tokens       │  │   Components    │  │    Patterns     │
│  ┌───────────┐  │  │  ┌───────────┐  │  │  ┌───────────┐  │
│  │  Colors   │  │  │  │  Button   │  │  │  │   Cards   │  │
│  │  Spacing  │  │  │  │   Card    │  │  │  │   Lists   │  │
│  │Typography │  │  │  │   Input   │  │  │  │   Forms   │  │
│  │  Effects  │  │  │  │   Badge   │  │  │  │  Dialogs  │  │
│  │  Motion   │  │  │  │    ...    │  │  │  │    ...    │  │
│  └───────────┘  │  │  └───────────┘  │  │  └───────────┘  │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

### 3.3 状态管理

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         状态管理策略                                     │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   Server State  │  │   Client State  │  │    URL State    │
│  ┌───────────┐  │  │  ┌───────────┐  │  │  ┌───────────┐  │
│  │   SWR /   │  │  │  │  useState │  │  │  │  Search   │  │
│  │   Fetch   │  │  │  │  useRef   │  │  │  │  Params   │  │
│  │  (Cache)  │  │  │  │  Context  │  │  │  │  (Filter) │  │
│  └───────────┘  │  │  └───────────┘  │  │  └───────────┘  │
│                 │  │                 │  │                 │
│  - API 数据     │  │  - UI 状态      │  │  - 筛选条件     │
│  - 资源列表     │  │  - 表单状态     │  │  - 分页状态     │
│  - 用户信息     │  │  - 模态框       │  │  - 排序方式     │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

---

## 四、数据库设计

### 4.1 ER 图

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│    Resource     │       │ ResearchProject │       │  ResearchSource │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id (PK)         │       │ id (PK)         │◀──────│ project_id (FK) │
│ type            │       │ name            │       │ id (PK)         │
│ title           │       │ description     │       │ type            │
│ url (UNIQUE)    │       │ status          │       │ title           │
│ content         │       │ created_at      │       │ content         │
│ summary         │       │ updated_at      │       │ embedding       │
│ score           │       └─────────────────┘       │ metadata        │
│ domain          │                                 └─────────────────┘
│ tags (JSONB)    │
│ is_featured     │       ┌─────────────────┐       ┌─────────────────┐
│ deep_research   │       │   ChatSession   │       │  ResearchOutput │
│ published_at    │       ├─────────────────┤       ├─────────────────┤
│ created_at      │       │ project_id (FK) │───────│ project_id (FK) │
└─────────────────┘       │ id (PK)         │       │ id (PK)         │
                          │ messages (JSONB)│       │ type            │
                          │ created_at      │       │ content         │
                          └─────────────────┘       └─────────────────┘
```

### 4.2 索引策略

```sql
-- 核心查询索引
CREATE INDEX idx_resources_type ON resources(type);
CREATE INDEX idx_resources_type_score ON resources(type, score DESC);
CREATE INDEX idx_resources_domain ON resources(domain);
CREATE INDEX idx_resources_score ON resources(score DESC);
CREATE INDEX idx_resources_published ON resources(published_at DESC);
CREATE INDEX idx_resources_featured ON resources(is_featured) WHERE is_featured = true;
CREATE INDEX idx_resources_language ON resources(language);
CREATE INDEX idx_resources_source_name ON resources(source_name);

-- 全文搜索索引
CREATE INDEX idx_resources_title_gin ON resources USING gin(to_tsvector('chinese', title));
CREATE INDEX idx_resources_summary_gin ON resources USING gin(to_tsvector('chinese', summary));

-- 向量索引 (pgvector)
CREATE INDEX idx_source_embedding ON research_sources USING ivfflat (embedding vector_cosine_ops);
```

---

## 五、外部服务集成

### 5.1 服务依赖图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Signal Hunter Backend                            │
└─────────────────────────────────────────────────────────────────────────┘
          │           │           │           │           │
          ▼           ▼           ▼           ▼           ▼
    ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
    │OpenRouter│ │  Tavily  │ │  Tingwu  │ │DashScope │ │ XGo.ing  │
    │          │ │          │ │          │ │          │ │          │
    │  Kimi K2 │ │  Search  │ │Transcribe│ │   TTS    │ │ Twitter  │
    │   LLM    │ │   API    │ │   API    │ │   API    │ │   RSS    │
    └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘
```

### 5.2 服务配置

| 服务 | 用途 | 环境变量 |
|------|------|---------|
| OpenRouter | LLM 调用 | `OPENAI_API_KEY` |
| Tavily | 搜索增强 | `TAVILY_API_KEY` |
| Tingwu | 音频转写 | `TINGWU_*` |
| DashScope | TTS 合成 | `DASHSCOPE_API_KEY` |
| Cloudflare R2 | 文件存储 | `R2_*` |

---

## 六、部署架构

### 6.1 Docker Compose 架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Docker Compose Network                           │
│                           (signal-network)                               │
└─────────────────────────────────────────────────────────────────────────┘
     │              │              │              │              │
     ▼              ▼              ▼              ▼              ▼
┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
│ Backend │   │Frontend │   │   DB    │   │  Redis  │   │pgAdmin  │
│  :8000  │   │  :3000  │   │  :5432  │   │  :6379  │   │  :5050  │
│         │   │         │   │         │   │         │   │(optional)│
│ FastAPI │   │ Next.js │   │PostgreSQL│   │  Cache  │   │  Tools  │
└─────────┘   └─────────┘   └─────────┘   └─────────┘   └─────────┘
     │              │              │              │
     └──────────────┴──────────────┴──────────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │    Volume Mounts    │
              ├─────────────────────┤
              │ signal-data         │
              │ signal-db           │
              │ signal-redis        │
              │ signal-logs         │
              └─────────────────────┘
```

### 6.2 生产部署架构

```
                    ┌─────────────────┐
                    │   Cloudflare    │
                    │     Pages       │
                    │   (Frontend)    │
                    └────────┬────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            Railway                                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐         │
│  │    Backend      │  │   PostgreSQL    │  │     Redis       │         │
│  │   (FastAPI)     │  │   (pgvector)    │  │    (Cache)      │         │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘         │
└─────────────────────────────────────────────────────────────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  Cloudflare R2  │
                    │    (Storage)    │
                    └─────────────────┘
```

---

## 七、安全架构

### 7.1 认证流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Client  │───▶│  Login   │───▶│  Verify  │───▶│  Issue   │
│          │    │  Request │    │ Password │    │   JWT    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                      │
     ┌────────────────────────────────────────────────┘
     ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│  Store   │───▶│  Attach  │───▶│  Access  │
│   JWT    │    │  Header  │    │ Protected│
│          │    │          │    │   API    │
└──────────┘    └──────────┘    └──────────┘
```

### 7.2 API 安全

| 机制 | 描述 |
|------|------|
| JWT | 用户认证令牌 |
| API Key | 服务间调用 |
| Rate Limiting | Redis 限流 |
| CORS | 跨域配置 |
| Input Validation | Pydantic 验证 |

---

## 八、监控与日志

### 8.1 健康检查

```
GET /health
{
  "status": "healthy",
  "database": "connected",
  "redis": "connected",
  "version": "3.0.0"
}
```

### 8.2 指标监控

| 指标 | 类型 | 描述 |
|------|------|------|
| `api_request_count` | Counter | API 请求计数 |
| `api_response_time` | Histogram | 响应时间分布 |
| `pipeline_run_count` | Counter | 采集运行次数 |
| `llm_token_usage` | Counter | LLM Token 消耗 |
| `cache_hit_rate` | Gauge | 缓存命中率 |

---

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
